package CreationalDesignPattern;

public class FactoryMethodDesignPattern {
    /* in this the object creation will depend upon the condition, if the condition satisfy than we can say that this particular object will create
    if an another condition satisfy than the Correspond object will create.
     */

}
